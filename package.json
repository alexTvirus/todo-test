{
  "name": "demo-complex-component",
  "version": "0.1.0",
  "private": true,
  "dependencies": {


    "axios": "^1.7.2",

    "json-server": "^1.0.0-beta.0"
    "path": "^0.12.7",

  },
  "scripts": {
    "start": "concurrently  \" json-server --watch ./JsonServer/db.json --port 3000 \" ",
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "engines": {
    "node": ">=16.x"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
